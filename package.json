{
  "name": "salary-management-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node seed.js",
    "build": "npm install --omit=dev && echo 'Build complete'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.22.2",
    "mongoose": "^8.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
